CREATE DATABASE fms_db;

-- Clients table 
CREATE TABLE clients (
id SERIAL PRIMARY KEY, 
name VARCHAR(100) NOT NULL, 
address TEXT, phone VARCHAR(20) ); 
-- Invoices table 
CREATE TABLE invoices ( 
id SERIAL PRIMARY KEY, 
invoice_no VARCHAR(20) UNIQUE NOT NULL, 
client_id INTEGER REFERENCES clients(id), 
client_name VARCHAR(100), 
phone VARCHAR(20), 
address TEXT, 
total NUMERIC(10,2) DEFAULT 0, 
status VARCHAR(20) DEFAULT 'unpaid', 
created_at TIMESTAMP DEFAULT NOW() ); 
-- Invoice items (each line on the invoice) 
CREATE TABLE invoice_items ( 
id SERIAL PRIMARY KEY, 
invoice_id INTEGER REFERENCES invoices(id) ON DELETE CASCADE, 
description VARCHAR(200), 
price NUMERIC(10,2) );

select * from invoices
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public';

ALTER TABLE invoice_items ADD COLUMN quantity INTEGER DEFAULT 1;

CREATE TABLE stock (
  id             SERIAL PRIMARY KEY,
  name           VARCHAR(200) NOT NULL,
  unit_price     NUMERIC(10,2) NOT NULL,
  quantity       INTEGER DEFAULT 0,
  category       VARCHAR(100),
  size           VARCHAR(50),
  extra          TEXT,
  created_at     TIMESTAMP DEFAULT NOW()
);

ALTER TABLE invoice_items ADD COLUMN stock_id INTEGER REFERENCES stock(id);

select * from stock

CREATE TABLE ledger (
  id            SERIAL PRIMARY KEY,
  customer_name VARCHAR(100) NOT NULL,
  invoice_id    INTEGER REFERENCES invoices(id) ON DELETE SET NULL,
  invoice_no    VARCHAR(20),
  debit         NUMERIC(10,2) DEFAULT 0,
  credit        NUMERIC(10,2) DEFAULT 0,
  note          TEXT,
  date          TIMESTAMP DEFAULT NOW()
);

ALTER TABLE invoices ADD COLUMN type VARCHAR(20) DEFAULT 'invoice';
ALTER TABLE invoices ADD COLUMN advance NUMERIC(10,2) DEFAULT 0;

SELECT * FROM ledger ORDER BY id DESC LIMIT 10;



CREATE TABLE IF NOT EXISTS expense_categories (
  id SERIAL PRIMARY KEY,
  name VARCHAR(50) NOT NULL UNIQUE,
  description TEXT
);

CREATE TABLE IF NOT EXISTS expenses (
  id SERIAL PRIMARY KEY,
  category_id INTEGER NOT NULL REFERENCES expense_categories(id) ON DELETE CASCADE,
  description TEXT NOT NULL,
  amount DECIMAL(12, 2) NOT NULL,
  date DATE NOT NULL,
  notes TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
select * from expenses

CREATE TABLE IF NOT EXISTS asset_categories (
  id SERIAL PRIMARY KEY,
  name VARCHAR(50) NOT NULL UNIQUE,
  description TEXT
);

CREATE TABLE IF NOT EXISTS assets (
  id SERIAL PRIMARY KEY,
  category_id INTEGER NOT NULL REFERENCES asset_categories(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  purchase_date DATE NOT NULL,
  purchase_cost DECIMAL(12, 2) NOT NULL,
  current_value DECIMAL(12, 2),
  depreciation_rate DECIMAL(5, 2),
  status VARCHAR(20) DEFAULT 'active',
  location VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS employee_types (
  id SERIAL PRIMARY KEY,
  type_name VARCHAR(50) NOT NULL UNIQUE,
  description TEXT
);

CREATE TABLE IF NOT EXISTS employees (
  id SERIAL PRIMARY KEY,
  employee_type_id INTEGER NOT NULL REFERENCES employee_types(id) ON DELETE CASCADE,
  first_name VARCHAR(100) NOT NULL,
  last_name VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE,
  phone VARCHAR(20),
  position VARCHAR(100),
  salary DECIMAL(12, 2) NOT NULL,
  hire_date DATE NOT NULL,
  status VARCHAR(20) DEFAULT 'active',
  department VARCHAR(100),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO expense_categories (name, description) VALUES
  ('Daily Expenses', 'Daily miscellaneous expenses'),
  ('Petrol', 'Fuel expenses'),
  ('WiFi', 'Internet and WiFi expenses'),
  ('Electricity', 'Electricity bills'),
  ('Gas', 'Gas expenses'),
  ('Maintenance', 'Building and equipment maintenance'),
  ('Carriage', 'Goods transport expenses'),
  ('Staff Transportation', 'Employee commute allowances'),
  ('General Office Expense', 'General office supplies and expenses')
ON CONFLICT DO NOTHING;

INSERT INTO asset_categories (name, description) VALUES
  ('Furniture', 'Office furniture and fixtures'),
  ('Machinery', 'Industrial and office machinery'),
  ('Building', 'Buildings and structures'),
  ('Moulds', 'Manufacturing moulds and tools')
ON CONFLICT DO NOTHING;

INSERT INTO employee_types (type_name, description) VALUES
  ('Company', 'Full-time company employees'),
  ('Sub-contract', 'Contract and temporary workers')
ON CONFLICT DO NOTHING;


CREATE INDEX IF NOT EXISTS idx_expenses_date ON expenses(date);
CREATE INDEX IF NOT EXISTS idx_expenses_category_id ON expenses(category_id);
CREATE INDEX IF NOT EXISTS idx_assets_category_id ON assets(category_id);
CREATE INDEX IF NOT EXISTS idx_assets_status ON assets(status);
CREATE INDEX IF NOT EXISTS idx_employees_type_id ON employees(employee_type_id);
CREATE INDEX IF NOT EXISTS idx_employees_status ON employees(status);
CREATE INDEX IF NOT EXISTS idx_employees_hire_date ON employees(hire_date);


















